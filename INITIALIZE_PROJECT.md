# 研究项目接入清单

完成标志是当前获准研究阶段能执行、保存、交付并接续，不是删除所有占位符。本文件用于从统一模板建立项目；接入完成后删除并作独立初始化提交。

## 1. 研究与阶段合同

沿用用户已有研究计划，明确问题、对象、方法、证据充分标准、阶段关系与正式成果。当前阶段细化到可检验成果；未来未知保留。负责人从已有决定生成阶段合同，只合并询问无法推得的研究选择与资源缺口。

普通交付默认auto，预定关键成果independent，显式人工检查点human。明确哪些阶段只是通报点、哪些要等用户；下一阶段获准范围不能靠计划列出来就推定。记录本地commit、push／发布、数据权限、资源与恢复边界。

## 2. 资产和运行能力

核对新项目origin身份，执行实际可用的setup，登记ResearchAssets输入、环境和每run输出。凭据留本机受保护位置，临时worktree不复制秘密。输入保护区分真实只读机制与同账号的软约定。

登记外置服务能力与项目白名单、阶段政策、事件接收者；未接通不能启动需跨会话的正式长作业。运行入口、代码快照、输出原子发布和通知去重均需实际验收，不只检查文件存在。

## 3. Multica与角色

核对服务端、daemon、Codex版本；父阶段直接指派普通Research Lead，子任务指派完整交付者。用已验证模型预设，不按名字推断能力。三项共享研究技能逐角色绑定，去除对应旧重复规范；不要把必需行为只放description或skill名称。

为跨阶段接续建立一个轻量项目推进总issue，阶段是它的子issue，成果是阶段的子issue；总issue在获准推进期保持in_progress。按计划建立父子关系和stage。有显式依赖按依赖分组；没有则按已确认顺序串行。必需子任务不得混在NULL stage中。首批输入与授权齐全才启动，其余backlog；只用一次assignment或mention路径。

## 4. 上下文及闭环验证

负责人和普通执行者分别指出适用计划、最新决定、数据与资源边界、成果用途；从真实工具轨迹核对技能正文读取。测试冷启动、正常续接、另一线程的新意见与编辑、会话不可恢复后的上下文重建。

用非付费演示验证长作业在源回合退出后存活，结果直接回原issue；异常退出也有一次事件。验证auto普通成果关闭后原生stage接续、human不越界、重复事件不重复计算。选定图表导出后验证干净检出可编译论文。

## 5. 开始研究

清理真实无用占位符，不为清零虚构科学决定；检查本次变更与相对链接，精确提交初始化文件。按获准范围派发首批任务，保留下一步责任。未验证能力明确列出，不把模板初始化当成全项目自动化已验收。
