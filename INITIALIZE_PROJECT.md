# 项目接入清单

本清单用于从模板创建项目后的首次接入。完成条件是当前授权阶段可以执行、交付并接续；不是仅删除占位符。完成后删除本文件，形成独立初始化提交，不将实质研究结果混入该提交。

## 1. 确认研究与有效授权

读取 [执行入口](AGENTS.md)、[研究计划](docs/plans/research-plan.md)、[项目偏好](docs/project-preferences.md) 和 [数据来源](docs/data-sources.md)。沿用用户已经明确的选择，不重复请求同一授权。

- 填写项目简介：研究问题、对象与分析单位、边界、正式交付。
- 写全研究阶段的目标、科学依赖、证据标准、成果呈现和解释边界；当前获准阶段细化为一个个可检验成果。
- 确认数据版本、方法、完整运行范围、资源上限、合理恢复范围和需要独立复核的交付物。
- 会实质改变设计或执行边界的缺口集中提交用户决定；未来阶段尚未确定的内容标为未知，不虚构方法和结论。

阶段有效授权写在 Multica 父任务；长期默认值写在项目偏好。初次尚无父任务时先形成待应用的任务预览。

## 2. 建立环境和资产映射

```text
python scripts/research.py setup
```

- `setup` 从 `origin` 识别仓库名，并在可见的 `ResearchAssets` 中建立映射、固定输入区和稳定环境；没有远端时传入 `--project-name`。
- 同名不同来源使用可读别名；已有仓库更名不自动搬动大型资产。仅在用户已选择其他磁盘时使用 `--asset-root`。
- 按实际需要调整 `pyproject.toml`，通过 `uv lock` 更新锁文件。MSSQL 才启用 `--extra mssql` 并安装匹配 ODBC 驱动。
- 只启用实际使用的数据源。凭据从系统环境或资产根的本机 `.env` 取得；不要要求每个工作树复制仓库内 `.env`。
- 检查本机持久执行能力；Linux、macOS、Windows 分别实测，不能用路径检查代替进程存活测试。论文工具的 Bash/Make/TeX 条件另行检查。
- 来源版本、许可、校验值及落点登记到数据来源文档。下载、查询和实际费用必须落在已有授权内。

`setup` 不自行创建线上任务或启动科学计算。目录说明无需逐个填写；仅改写项目确实变化的约定。

## 3. 接入 Multica 与桥接服务

本步骤需要工作区管理员已提供配套桥接程序及其部署说明。模板的 `setup` 只准备项目环境，不会部署工作区服务。尚未部署时，由管理员先将桥接代码固定到稳定位置并交给系统服务托管：Linux 使用配套用户服务安装器；macOS/Windows 使用各自系统工具，分别验证，不把运行工具的三平台测试当作桥接服务验收。

- 配置项目的仓库、工作树模式及简短背景，项目入口回指根指令、研究计划和数据说明。
- 使用已有六个模型预设，按任务难度直接选择成员；不为单次任务临时改共享模型配置。
- 研究汇报、研究任务交付技能逐成员绑定；阶段协调绑定领队。检查普通成员的实际技能配置，不能只检查小队指令。
- 准备父子任务映射：父任务是一项可人工验收的研究模块；子任务写目的、输入版本、前置成果、完整范围、产物、检查和恢复边界。
- 将真实依赖映射到 `stage` 批次；缺少显式依赖时按计划顺序固定为串行。预览中所有父子任务负责人固定为 Butler，另列评论派工的候选成员、阶段授权、必要复核和自动完成策略。
- 按已有授权应用预览。创建时先不指定负责人，再按返回版本指派 Butler 并禁止启动，之后不随成员分工改负责人。后继留在 `backlog`；首批前置产物和授权齐备时，由 Butler 在同一 issue 评论提及成员派工。成员完成、失败或有差距均评论交回 Butler；Butler 无实质决定则继续派工，需要决定或阶段验收则提及用户。

完成任务预览后，由管理员接通这批任务的结果通知和收尾：

1. 在受保护的本机策略配置中逐项登记当前项目、阶段父任务、子任务及预期父子关系、有效授权、必要复核和固定资产根。只有明确允许的子任务采用自动完成，阶段父任务保持人工验收；未登记项目或任务默认手动、未授权。执行成员不能自行扩大策略范围。
2. 为结果通知绑定原生 `run_only` webhook 及已登记的接收者。它只接收固定运行结果、核验原 issue 和最新意见，再向原 issue 的固定负责人 Butler 发送一次带事件标记的唤醒评论；由 Butler 在真正属于原 issue 的执行中检查、继续派工或交付。不能让独立 webhook 会话直接提交该研究 issue 的最终交付。
3. 将长期成员凭据、webhook 密钥和策略文件保存在本机受保护位置，限制文件权限或用户 ACL，不进入仓库、运行记录和日志。先预览策略，再按已有授权启用当前范围，并确认系统服务持续运行、重启后仍保留事件记录。未部署服务或仅创建 webhook 都不算通知已经接通。
4. 记录项目绑定的桥接部署入口和管理员维护方式，不复制密钥或另一套任务表。需要核对策略时读取该部署的真实配置，不以仓库文字替代生效配置。

Multica 是任务状态与执行依赖的来源；仓库不另建实时依赖表。暂停和旧停止点不得因接入而解除。

## 4. 验证真实阅读和完整演示

分别安排领队和一名普通成员读取上下文，要求其指出适用计划章节、授权边界、输入版本和成果用途。文件存在或资源已挂载不算读入成功。

用无真实付费请求的小规模演示验证：

1. 两个工作树映射到同一固定输入区，输出分属不同运行目录。
2. 已提交的代码在外部稳定位置执行，退出会话并移除执行工作树后仍能完成。
3. 完成事件经 `run_only` 接收者返回原 issue，确认实际产生属于该 issue 的接续执行，而不只检查 webhook 受理成功。执行成员先将结果或失败评论交回 Butler；Butler 检查完整范围和研究影响，按交付流程进入 `in_review` 并发布绑定最新版本的正式记录。
4. 已登记可自动完成的子任务通过检查后关闭，父任务接到后续运行；有独立复核要求的任务先固定草稿和产物、复核后完成同一交付。阶段结束停在人工验收。
5. 从派工、成员回报到下一次派工，父子 issue 的负责人始终为 Butler；验证一次失败或交付不完整也会交回 Butler，并正确继续或提及用户。新评论、暂停、重复事件、离线与恢复不导致误关闭、重复派工或重复付费计算。
6. 按 Multica 快照生成看板；短文字能独立说明结论。imagegen 仅在运行时已实测可用时接入，不把此能力当作初始化的隐含事实。
7. 选定图表 `export` 后具备来源索引；干净检出可以编译论文。

演示只验证流程，不能代替当前研究任务的全量交付。未实测的平台和集成写明未验收。

## 5. 收尾

- 检查剩余 `PROJECT-INIT`、`待初始化` 和待确认项；必要未知保留并注明影响，不为清零而补造。
- 确认当前阶段可执行或记录真实阻塞，后继任务和阶段验收点正确。
- 检查相对链接、`git diff --check` 和 `git status --short`，确认凭据及研究数据没有进入版本控制。
- 删除本清单，显式选择初始化文件提交；遵守本机署名与当前推送授权。
