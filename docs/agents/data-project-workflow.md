# 数据项目流程

## 目标

为每个数据集提供可复现的采集与处理链。

## 数据分层

- `data/raw/`: 原始源数据（只读）。
- `data/interim/`: 清洗前临时产物（半成品）。
- `data/processed/`: 可直接分析的数据。
- `data/external/`: 外部源引用或中间快照。

## 最小流程

1. 新增数据源：记录来源与更新频率。
2. 写入清单：时间、版本、下载方式、校验信息。
3. 在实验中引用固定版本。
4. 保存每一步变换脚本和参数。
