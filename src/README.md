# 源码组织

`src/` 存放可复用项目代码。

按真实功能模块组织，不要在项目上下文还不明确时先发明泛化目录。

可选示例：

- `src/ingest/`：数据源客户端和加载器。
- `src/cleaning/`：清洗和标准化逻辑。
- `src/analysis/`：分析流程。
- `src/figures/`：可复用绘图辅助函数。
- `src/utils/`：小型共享工具。

流程编排放在 `scripts/`。

如果脚本里的逻辑开始可复用，把逻辑移到 `src/`，脚本只保留薄封装。
