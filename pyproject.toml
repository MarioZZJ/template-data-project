[project]
name = "quantitative-research-project"
version = "0.1.0"
description = "Reproducible single-study quantitative research project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0.3",
    "pyodbc>=5.2.0",
    "python-dotenv>=1.0.1",
]

[tool.uv]
package = false
