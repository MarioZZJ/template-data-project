# 复制为 .env 后填写实际值；.env 不提交。

PROJECT_NAME=titanic-survival-associations
DATA_ROOT=./data
OUTPUT_ROOT=./outputs

# Kaggle：也可使用 kaggle auth login 或用户目录中的 token 文件。
KAGGLE_API_TOKEN=
KAGGLE_TIMEOUT_SECONDS=60
# 仅当网络重置 storage.googleapis.com 时启用受信任的 TLS 主机回退。
KAGGLE_STORAGE_TLS_HOST=
