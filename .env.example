# Optional per-project credentials. Copy to the asset root .env, not a worktree.
# setup prints the asset root; this file and credentials are never recorded in Git.
# System environment values take precedence over this file.
# Enable only data sources used by the project. Never put secrets in command arguments.
# DATA_ROOT, RUN_DIR and OUTPUT_ROOT are injected independently for each run.
